<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.ictedu.movie.login.LoginDao">

<!-- Login Check -->
	<select id="checkPassword" parameterType="mem" resultType="mem">
		SELECT email, password, regdate
		FROM member
		WHERE email = #{email} AND password = #{password}
	</select>

	<!-- Search for User Information -->
	<select id="getUserInfo" parameterType="mem" resultType="mem">
		SELECT email, password, regdate
		FROM member
		WHERE email = #{email}
	</select>
	
	<!-- Password Update -->
	<update id="updatePassword">
		UPDATE member
		SET password=#{password}
		WHERE email = #{email}
	</update>
	
	
	<!-- User Deletion -->
	<delete id="withdrawUserInfo">
		DELETE FROM member
		WHERE email = #{email}
	</delete>

	<!-- Password Change -->
	<update id="changepw">
		UPDATE member
		SET password = #{password}
		WHERE email = #{email}
	</update>

</mapper>